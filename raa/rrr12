#include <stdio.h>

int main(void)
{
    char a[10];
   scanf("%s",a);
        if(a[0]=='s' || a[0]=='s')
        {
            printf("\nyes");
        }
        else
        {
            printf("\nno");
        }
    return 0;
}
