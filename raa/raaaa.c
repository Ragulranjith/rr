#include <stdio.h>
int main()
{
    char a[50];
    printf("enter the day:");
    scanf("%s",&a);
        if(a[0]=='s' || a[0]=='s')
        {
            printf("\nyes");
        }
        else
        {
            printf("\nno");
        }
    return 0;
}
